name: janken
description: A rock, paper, scissors, lizard, spock command-line game
version: 1.0.0
repository: https://github.com/GuilhermeAlecrim7K/janken-cmd.git

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
# dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
